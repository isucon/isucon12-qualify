name: Full Test
on: [push]
jobs:
  test:
    strategy:
      matrix:
        go:
          - 1.18
    name: Build
    runs-on: self-hosted
    container:
      image: ubuntu:20.04
      options: "--cpus 2"
    services:
      db:
        image: mysql:8.0.29
        ports:
          - 3306:3306
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_ROOT_HOST: "%"
        options: >-
          --health-cmd "mysqladmin ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - name: show cpus
        run: echo "### Benchmark CPUs\n $(nproc) CPUs\n" >> $GITHUB_STEP_SUMMARY
      - name: Set up Go
        uses: actions/setup-go@v2
        with:
          go-version: ${{ matrix.go }}
        id: go

      - name: Check out code into the Go module directory
        uses: actions/checkout@v3

      - name: setup tools
        run: |
          apt-get update && apt-get install -y mysql-client-8.0 sqlite3

      - name: "build initial data"
        working-directory: ./data
        run: |
          mysql -uroot -proot --host 127.0.0.1 < ../webapp/sql/admin/01_create_mysql_database.sql
          make build
          make install

      - name: "run webapp"
        working-directory: ./webapp/go
        run: |
          make
          ./isuports &

      - name: "run bench"
        working-directory: ./bench
        run: |
          make
          ./bench -target-addr 127.0.0.1:3000 -target-url http://t.isucon.dev
